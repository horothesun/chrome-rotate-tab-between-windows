{
  "name": "Move Tab to Next Window",
  "version": "1.0",
  "description": "Move the current tab to another open window with a keyboard shortcut.",
  "manifest_version": 3,
  "permissions": ["tabs"],
  "background": {
    "service_worker": "background.js"
  },
  "commands": {
    "move_tab": {
      "suggested_key": {
        "default": "Ctrl+Shift+K",
        "mac": "Command+Shift+K"
      },
      "description": "Move the current tab to the next window."
    }
  }
}
